# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HackatonApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HackatonApp
  pod 'FCAlertView'
  pod 'SDWebImage', '~> 4.0'
  pod 'BluemixAppID'
  pod 'Locksmith'
end
